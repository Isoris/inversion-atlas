{
  "name": "inversion-atlas",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "inversion-atlas — ES-module flag so 'node atlases/inversion/shared/test_*.js' can load the page modules under test. There is no npm dependency graph; the browser imports atlases/inversion/**/*.js directly."
}
