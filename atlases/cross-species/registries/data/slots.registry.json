{
  "_doc": "State slots this atlas owns or shares. Phase 1a: declares state.crossSpecies.activeBreakpointId (cross-page focal selection) — used by all 3 pages.",
  "atlas_id": "cross-species",
  "version": "1.0",
  "slots": [
    {
      "slot_id": "crossSpecies.activeBreakpointId",
      "owner_page": "bp_catalogue",
      "consumers": ["bp_atlas_reciprocity", "bp_atlas_arcs"],
      "schema": { "type": ["string", "null"], "description": "cluster_id from breakpoints_consolidated_v1" }
    }
  ]
}
